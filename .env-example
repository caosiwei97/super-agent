# OpenAI-compatible model
OPENAI_API_KEY=your-key
MODEL_BASE_URL=https://api.deepseek.com
MODEL_ID=deepseek-v4-flash
MODEL_CONTEXT_WINDOW=16000

# Optional DeepSeek proxy billing override (USD / 1M tokens; set all four together)
# MODEL_INPUT_PRICE_PER_MILLION=0.14
# MODEL_OUTPUT_PRICE_PER_MILLION=0.28
# MODEL_CACHE_WRITE_PRICE_PER_MILLION=0.14
# MODEL_CACHE_READ_PRICE_PER_MILLION=0.0028

# Agent runtime
TOKEN_BUDGET=1000000
TI_AGENT_WORKSPACE=.

# GitHub hosted MCP
GITHUB_PERSONAL_ACCESS_TOKEN=
